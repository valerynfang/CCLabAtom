let cam;


function setup() {
  canvas = createCanvas(640, 480);
  canvas.parent(canvasContainer)
  // cam = createCapture(VIDEO);
  // cam.hide();
  background(255);

}

function draw() {

  //console.log(millis());
  // secondPassed = (${round(millis() / 1000)}`);
  // if (secondPasses<5){
  //   image(cam, 0, 0);
  //   filter(GRAY);
  // } else {
  //   background(255)
  // }


}
